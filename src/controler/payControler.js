const express = require("express");


//here come verification to, check if all filds its ok, 
// then, call next, if not or bad formated, make a new throw then send error mesage to user
const payControler = function (req, res, next) {
};
module.exports = payControler;
